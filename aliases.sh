alias todo-back='docker-compose up --build backend'
alias todo-front='docker-compose up --build front'
alias todo-front-bash = 'docker exec -it todo_front bash'
alias todo-front-bash = 'docker exec -it todo_back bash'